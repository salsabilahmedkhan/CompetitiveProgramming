#include <stdio.h>
#include <math.h>
int main()
{
    long long int b,p,n,i,j,mod;
    while(scanf("%lld %lld %lld",b,p,n)!=EOF)
    {
        mod=1;
        while(p>=0)
        {
            if(p%2==0)
            {

            }
        }
    }

    return 0;
}
